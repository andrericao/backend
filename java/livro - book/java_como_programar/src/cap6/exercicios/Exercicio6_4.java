package cap6.exercicios;

// 6.4Forneça o cabeçalho de método para cada um dos seguintes métodos.
// a) O método hypotenuse, que aceita dois argumentos de ponto flutuante
// de precisão dupla side1 e side2 e retorna um resultado de ponto flutuante de dupla precisão.

// b) O método smallest, que recebe três inteiros x, y e z e retorna um inteiro.

// c) O método instructions, que não aceita nenhum argumento e não retorna um valor.
// [Observação: esses métodos são comumente utilizados para exibição de instruções para o usuário.]

// d) O método intToFloat, que recebe um argumento number do tipo inteiro e retorna um float.
public class Exercicio6_4 {

    // a)
    public double hypotenuse(double side1, double side2){
        return 0.0;
    }

    // b)
    public int smallest(int x, int y, int z){
        return 0;
    }

    // c)
    public void instructions(){}

    // d)
    public float intToFloat(int number){
        return 0.0F;
    }
}
